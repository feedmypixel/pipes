# Tasks: Design Foundation

From `tasks/prd-design-foundation.md`. The shared token + primitive layer every surface
sits on. Design source of truth: `design/v1/` (`README.md` spec, `assets/pipes.css` exact values).

## Tasks

- [ ] 0.0 Create feature branch
- [ ] 1.0 Global tokens + base stylesheet (lifted from `design/v1/assets/pipes.css`)
- [ ] 2.0 Theme handling (auto `prefers-color-scheme` + dev-only light/dark/system switcher)
- [ ] 3.0 Lucide setup + shared primitives (`StatusIcon`, `RefChip`, `RelativeTime`, `Row`)
- [ ] 4.0 Dev-only component showcase (every primitive × states × both themes)
- [ ] 5.0 Verify icons + quality gates (check/lint/test green; dev-only code excluded from prod)
