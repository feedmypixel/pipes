// TEMPORARY: fail #41's CI to fire the enriched PR-failure notification. Reverted after.
test('TEMP demo: intentional CI failure (delete me)', () => {
  expect('pipes-notification-demo').toBe('green')
})
