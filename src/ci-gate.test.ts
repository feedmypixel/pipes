// TEMPORARY: intentional failure to verify the CI merge gate blocks the PR.
// Remove this file once the failing-check behaviour is confirmed.
test('intentional CI failure — remove this file', () => {
  expect(1).toBe(2)
})
