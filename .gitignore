# Dependencies
node_modules/

# Build output
dist/
*.zip

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local secrets — never commit a token. Config lives in chrome.storage at runtime,
# but ignore any local env files you might add during development.
.env
.env.local
